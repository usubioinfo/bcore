<!DOCTYPE html>
{% set bs4 = false %}
<html lang="en">
	{% import "universal/_header-macro.njk" as headerMacro %}
	{{ headerMacro.header(bs4=bs4, scss=false) }}
	<body>
		{% include "universal/_preload.njk" %}
		<div id="page" class="page">
			<div id="content-wrapper" class="content-wrapper">
				<div class="content-wrapper-inner" style="background: #efefef;">
					{% from "universal/_navbar-switch.njk" import navbar %}
					{{ navbar(bs4=bs4) }}
					<div class="row manual-container">
						<div class="col-xs-12">
							<span class="manual-page-title">Manuals</span>
						</div>

						{% setAsync('pdfs', getPDFs, []) %}
						{% for pdf in pdfs %}
							<div class="col-sm-6">
								<div class="col-xs-12 hide-overflow">
									<a href="{{ pdf.link }}" title="{{ pdf.name }}" target="_blank" rel="noopener noreferrer" class="pdf-title">
										{{ pdf.name }}
									</a>
								</div>
							</div>
						{% else %}
						  <div>No Manuals</div>
						{% endfor %}
					</div>

				</div><!-- Footer start -->
				{% from "universal/_footer-switch.njk" import footer %}
				{{ footer(bs4=bs4) }}
			</div>
		{% include "universal/_footer-scripts.njk" %}
		</div>
	</body>
</html>
